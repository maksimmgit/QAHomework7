В этом задании логика проверки заключалась в том, чтобы брать список значений из .features и сравнивать её методом compareTO
со списком, который собрали из атрибута innerHTML в окне автобарахолки или домов-дач.
Проверка выполняется с помощью assertTrue, если есть хотя бы одно несовпадение, то сразу шлём false не добивая список.
Можно улучшить и считать количество false, чтобы выводить их, выводить List id, имя и ошибку.
Драйвер вынес в класс Base.
Не разобрался как закрыть браузер после выполнения двух сценариев? То есть смысл в том, чтобы открыть один раз браузер,
прогнать сценарии и после этого закрыть. Не понял куда класть метод.
